# 全バックエンドサービス共通シークレット
# 本番環境では ExternalSecret (AWS Secrets Manager) に差し替える
apiVersion: v1
kind: Secret
metadata:
  name: app-secret
  namespace: workout-backend
type: Opaque
stringData:
  JWT_SECRET: "changeme_generate_with_openssl_rand_hex_32"
