apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: workout-data
spec:
  clusterIP: None  # Headless Service for StatefulSet
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
